language: python
python:
  - "3.6"
intall:
  - pip install -r requirements.txt
  - pip install pytest
  - pip install pytest-cov
  - pip install python-coveralls
script:
  - pytest --cov run
after_success:
  - coveralls